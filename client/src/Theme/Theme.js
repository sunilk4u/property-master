import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    light: {
      main: "#C996CC",
    },
    light2: {
      main: "#916BBF",
    },
    dark: {
      main: "#3D2C8D",
    },
    dark2: {
      main: "#1C0C5B",
    },
  },
});

export default theme;
